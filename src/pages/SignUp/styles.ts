import styled from 'styled-components';
import { Container as SignContainer } from '../SignIn/styles';

export const Container = styled(SignContainer)`
  justify-content: space-evenly;
  gap: 10px;
`;

export const Form = styled.form`
  margin-top: 20px;
  width: 100%;
`;
